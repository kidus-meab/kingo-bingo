BOT_ACCESS_TOKEN=

DATABASE_URL=file:./prisma/kingo.db

# Optional: only needed when running the standalone Hono process (`bun server/index.ts`).
# Normal `bun run dev` serves /api through Next.js + Hono.
API_PORT=4000
